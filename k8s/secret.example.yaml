# Example only. Do not apply directly.
# Copy to secret.local.yaml and replace both values before applying.
apiVersion: v1
kind: Secret
metadata:
  name: java-cloud-platform-lab-datasource-secret
  labels:
    app: java-cloud-platform-lab
type: Opaque
stringData:
  SPRING_DATASOURCE_USERNAME: replace-me
  SPRING_DATASOURCE_PASSWORD: replace-me
